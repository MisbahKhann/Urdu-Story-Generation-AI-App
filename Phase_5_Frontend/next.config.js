/** @type {import('next').NextConfig} */
const nextConfig = {
  // Backend URL injected at build/runtime via env var
  // Set NEXT_PUBLIC_API_URL=https://your-backend.onrender.com in Vercel dashboard
};

module.exports = nextConfig;